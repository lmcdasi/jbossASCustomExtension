module=The configuration of the CUSTOM MODULE subsystem
module.add=Operation that adds the CUSTOM MODULE subsystem

activation=How the CUSTOM subsystem is activated. Possible values (default): eager.

#config=CUSTOM MODULE Configuration Admin information. 
#config.add=Add CUSTOM MODULE Configuration Admin configuration, the primary key of the configuration represents the PID.
#config.remove=Remove CUSTOM MODULE Configuration Admin configuration.
#config.entries=The configuration data associated with the PID.

#property=CUSTOM MODULE Framework properties.
#property.add=Add CUSTOM MODULE Framework property.
#property.remove=Removes an CUSTOM MODULE Framework property.
#property.value=The value of the CUSTOM MODULE Framework property.

module=Pre-loaded modules and bundles.
module.add=Add pre-loaded module.
module.remove=Remove a pre-loaded module.
module.startlevel=Sets the startlevel of a pre-loaded module. Setting the start level causes the module to be started once the relevant framework startlevel has been reached. Specifying the start level only applies to modules which are CUSTOM MODULE bundles.
